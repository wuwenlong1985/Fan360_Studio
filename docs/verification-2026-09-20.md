# 2026-09-20 渲染与工作台更新

## 修复和调整

- 修复 `PolarCapture` 的正优先级 `useFrame` 禁用屏幕自动绘制的问题，设备采样与视口绘制同时运行；采样结束恢复渲染目标、舞台可见性及背景。
- 窗口创建后立即显示应用外壳，避免隐藏窗口等待首次绘制、Canvas 尺寸测量又依赖首次绘制的启动等待问题。
- 倒计时、3D 文字和模型标签改用能够读取现有 JSON 字形文件的立体文字组件。加载等待限制在相应场景内部。
- 窗口默认宽度 1600 → 2080（+30%），标题内容区 60 → 72 px（+20%），原生窗口按钮区域高度 40 → 48 px。窗口适配显示器可用工作区。
- 顶部保留 LOGO/名称、场景选择、导出和“下载到设备”，删除副标题及其他顶部控件。
- 三个显示开关移到右侧“显示”设置，右栏收起后保留设置入口。项目操作和录制分别位于“场景与模型”和“动画”。
- 移除装饰网格和线框，使用本地 HDR 环境、自然主辅光、软阴影及 ACES 色调映射。
- 改善八项实物场景：草莓、人物雕像、地球、礼盒、玫瑰、宇航员、钻石、跑车。资产和 Draco 解码器均可离线加载；编辑舞台不进入设备帧。

## 验证

- TypeScript 类型检查：通过。
- 核心测试：17/17 通过。
- Electron 构建：通过。
- 真实 Electron + WebGL 离线渲染回归：20/20 场景可见、持续动画、产生有效 115,200 B 设备帧。
- 暂停/继续、设备预览、右栏/下方/圆框开关以及收起后恢复右栏：通过。
- 顶部 72 px 高度、单一场景选择器及两个操作按钮：通过。
- 渲染异常和资源加载错误：0。
- 实际窗口和实物场景截图已检查，见 README。

回归命令：`npm run typecheck`、`npm test`、`npm run test:render`。脚本会保留截图及 JSON 报告到 `test-artifacts/render-built/`。

## 验证边界

验证覆盖桌面软件渲染、采样、交互和本地构建。此次未连接真实风扇设备；未据此声称硬件显示效果、TCP 实机链路或转速时序通过。程序化实物模型增强了形体和材质，但不等同于照片级扫描资产。
